/**
 *
 * Task:
 *
 * Prepare a Basket class that will handle 2 types of objects:
 * AuctionItem
 * and
 * BuyNowItem
 *
 * by implementing the following implementation logic:
 */

// Remove comments (/* */) before start work on the task:
/*

const myBuyNowBasket = new Basket<BuyNowItem>();
const myAuctionBasket = new Basket<AuctionItem>();

myBuyNowBasket.addItem(new BuyNowItem('television set'));
myBuyNowBasket.addItem(new BuyNowItem('Couch'));

myAuctionBasket.addItem(new AuctionItem('Trading card'));

myBuyNowBasket.getAllItems().forEach((item: BuyNowItem) => {
    console.log(item.name);
})

myAuctionBasket.getAllItems().forEach((item: AuctionItem) => {
    console.log(item.name);
})

// [!] Extra points:
// if this operation is not possible in TS (TS will tell us about mistake below):
// myBuyNowBasket.getAllItems().push(new BuyNowItem('Troll'))


 */

export {};
